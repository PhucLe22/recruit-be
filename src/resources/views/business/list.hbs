<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Danh sách doanh nghiệp</title>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', sans-serif;
      background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
      min-height: 100vh;
      padding: 20px;
    }

    .business-card {
      background: white;
      border-radius: 10px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      margin-bottom: 20px;
      overflow: hidden;
    }

    .business-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
    }

    .business-logo {
      width: 100%;
      height: 200px;
      object-fit: cover;
      border-bottom: 1px solid #eee;
    }

    .business-info {
      padding: 20px;
    }

    .business-name {
      font-size: 1.5rem;
      font-weight: 600;
      margin-bottom: 10px;
      color: #333;
    }

    .business-description {
      color: #666;
      margin-bottom: 15px;
      font-size: 0.95rem;
    }

    .business-meta {
      display: flex;
      justify-content: space-between;
      color: #888;
      font-size: 0.9rem;
      margin-bottom: 15px;
    }

    .verified-badge {
      background: #4CAF50;
      color: white;
      padding: 3px 8px;
      border-radius: 12px;
      font-size: 0.75rem;
      font-weight: 600;
      display: inline-flex;
      align-items: center;
    }

    .view-btn {
      width: 100%;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      border: none;
      padding: 10px;
      border-radius: 5px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .view-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
    }

    .page-header {
      text-align: center;
      margin-bottom: 40px;
    }

    .page-header h1 {
      font-size: 2.5rem;
      font-weight: 700;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      margin-bottom: 15px;
    }

    .page-header p {
      color: #666;
      font-size: 1.1rem;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="page-header">
      <h1>Danh sách doanh nghiệp</h1>
      <p>Khám phá các doanh nghiệp hàng đầu đang tuyển dụng</p>
    </div>

    <!-- Search and Filter Section -->
    <div class="card mb-4">
      <div class="card-body">
        <form action="/business" method="get" class="row g-3">
          <div class="col-md-6">
            <div class="input-group">
              <span class="input-group-text"><i class="fas fa-search"></i></span>
              <input 
                type="text" 
                name="search" 
                class="form-control" 
                placeholder="Tìm kiếm doanh nghiệp..."
                value="{{search}}"
              >
            </div>
          </div>
          <div class="col-md-4">
            <div class="input-group">
              <span class="input-group-text"><i class="fas fa-map-marker-alt"></i></span>
              <input 
                type="text" 
                name="location" 
                class="form-control" 
                placeholder="Thành phố"
                value="{{location}}"
              >
            </div>
          </div>
          <div class="col-md-2">
            <button type="submit" class="btn btn-primary w-100">
              <i class="fas fa-filter"></i> Lọc
            </button>
          </div>
        </form>
      </div>
    </div>

    {{#if list.length}}
      <div class="row">
        {{#each list}}
        <div class="col-md-4 mb-4">
          <div class="business-card h-100">
            <img 
              src="{{this.logoPath}}" 
              alt="{{this.companyName}}" 
              class="business-logo"
              onerror="this.onerror=null; this.src='/images/default-company-logo.png'"
            >
            <div class="business-info d-flex flex-column h-100">
              <h3 class="business-name">{{this.companyName}}</h3>
              <p class="business-description flex-grow-1">
                {{#if this.description}}
                  {{truncate this.description 120}}
                {{else}}
                  Chưa có mô tả
                {{/if}}
              </p>
              <div class="business-meta">
                <span><i class="fas fa-map-marker-alt"></i> {{this.location}}</span>
                {{#if this.isVerified}}
                <span class="verified-badge"><i class="fas fa-check-circle"></i> Đã xác minh</span>
                {{/if}}
              </div>
              <a href="/jobs/{{this.slug}}" class="view-btn mt-3">Xem chi tiết</a>
            </div>
          </div>
        </div>
        {{/each}}
      </div>

      <!-- Pagination -->
      {{#if (gt totalPages 1)}}
      <nav aria-label="Page navigation" class="mt-4">
        <ul class="pagination justify-content-center">
          {{#if (gt currentPage 1)}}
            <li class="page-item">
              <a class="page-link" href="?page={{sub currentPage 1}}{{#if search}}&search={{search}}{{/if}}{{#if location}}&location={{location}}{{/if}}" aria-label="Previous">
                <span aria-hidden="true">&laquo;</span>
              </a>
            </li>
          {{/if}}
          
          {{#each (paginationRange currentPage totalPages)}}
            <li class="page-item {{#if (eq this currentPage)}}active{{/if}}">
              <a class="page-link" href="?page={{this}}{{#if ../search}}&search={{../search}}{{/if}}{{#if ../location}}&location={{../location}}{{/if}}">
                {{this}}
              </a>
            </li>
          {{/each}}
          
          {{#if (lt currentPage totalPages)}}
            <li class="page-item">
              <a class="page-link" href="?page={{add currentPage 1}}{{#if search}}&search={{search}}{{/if}}{{#if location}}&location={{location}}{{/if}}" aria-label="Next">
                <span aria-hidden="true">&raquo;</span>
              </a>
            </li>
          {{/if}}
        </ul>
      </nav>
      {{/if}}
    {{else}}
      <div class="text-center py-5">
        <i class="fas fa-building fa-4x text-muted mb-3"></i>
        <h3>Không tìm thấy doanh nghiệp nào</h3>
        <p class="text-muted">Hãy thử thay đổi bộ lọc tìm kiếm</p>
        <a href="/business" class="btn btn-primary mt-3">
          <i class="fas fa-undo"></i> Xem tất cả doanh nghiệp
        </a>
      </div>
    {{/if}}
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
